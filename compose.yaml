services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: patient_demo
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d patient_demo"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/patient_demo
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: postgres
    ports:
      - "8080:8080"

  data-loader:
    image: postgres:16
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    environment:
      PGPASSWORD: postgres
    volumes:
      - ./dump.sql:/data/dump.sql:ro
      - ./scripts/import-data.sh:/scripts/import-data.sh:ro
    command: ["sh", "/scripts/import-data.sh"]
